# PR review threads — live-PR findings

Recorded answers to the empirically-verified-facts block cited by
`scripts/lib/prt/gh.sh:13-21`, and to the V1-V7 spike questions from the Phase 2 plan. Each row
is a question that can only be answered by observing a real PR/merge-queue run against GitHub —
not by reading the code or the docs. This file is the scaffold; **do not fill in an answer
without having actually run the spike.** Rows stay `PENDING` until then.

V1 is answered by Phase 2 Task 4 (requires `merge_group:` wired into a real merge queue — see
Task 3). V3-V7 are answered by Phase 2 Task 5, run against a `.github` scratch PR once
`continue-on-error: true` is removed from `pr-review.yml`. **V2 is the one exception to "not by
reading the code or the docs"**: it is answered from GitHub's own documented reusable-workflow
context-resolution behavior, not a live spike — `.github` calls its own workflow, so caller and
callee are the same repo there and no live test can distinguish them; see the V2 row for the
citation and the residual caveat. **V6 is answered, but reveals a live permission defect**: the
default `GITHUB_TOKEN` cannot resolve a review thread it created (`viewerCanResolve:false`), so
bot-driven auto-resolve does not currently work in production — see the V6 row.

| # | Question | Answer | Recorded | Evidence |
|---|----------|--------|----------|----------|
| V1 | Is the required-check context string (`pr-review / AI Code Review`) byte-identical on a `merge_group` run as on a `pull_request` run? | **Yes.** Checks API returns `name: "pr-review / AI Code Review"` on both events, byte-identical. On `merge_group` the job reports `conclusion: skipped` (not missing) — the callee's draft/fork `if:` evaluates false since `github.event.pull_request` doesn't exist on that event, matching the predicted no-op. | 2026-08-18 | go-kure/kure#669 enqueued via GitHub merge queue; run `32142799469` on ref `gh-readonly-queue/main/pr-669-6b065743775a886f7fecbfa1bae2151859d65e8c`, check-run `95729260649` (`pr-review / AI Code Review`, `skipped`). Compared against the same context name on the `pull_request` event at commit `559ecb7` (kure#669's own head). |
| V2 | Does `vars.PR_REVIEW_THREADS_MODE` resolve inside a *called* reusable workflow, and against which repo (caller's or callee's)? | **Caller's repo** (kure/launcher), not the callee (`.github`) that defines the workflow file — this is a documented GitHub Actions property, not directly observable from inside `.github`'s own dogfood PR (it calls itself, so caller==callee there). Consequence: an incident-mode repo-level override must be set on the affected consumer, not on `.github`. | 2026-08-18 | docs.github.com/en/actions/reference/workflows-and-actions/variables: "For reusable workflows, the variables from the caller workflow's repository are used. Variables from the repository that contains the called workflow are not made available to the caller workflow." Not independently confirmed by a live kure/launcher test this round — recommend a follow-up live spike there before removing this caveat entirely. |
| V3 | Does an outdated-but-unresolved review thread still block merge? | **Yes, though this run's own thread never actually went `isOutdated: true`.** A thread whose anchored line was pushed past by an unrelated content shift stayed `isOutdated: false` — GitHub's diff-position tracking re-anchored it to the new line automatically — and remained gating regardless; `mergeStateStatus` stayed `BLOCKED` throughout, so the "still blocks" answer holds, but this row does not itself demonstrate a `true`-outdated thread blocking merge — V4's threads (`isOutdated: true, line: null`) are the actual evidence for that stronger claim. Side finding: the context shift changed the finding's fingerprint, so the script opened a *second*, duplicate thread for the same underlying bug rather than recognizing it as the same finding — dedupe is context-sensitive, not purely content-sensitive. | 2026-08-18 | go-kure/.github#58 (scratch, closed unmerged), thread `PRRT_kwDOSHZe8M6aJdFB` at commit `a917567` — GraphQL `reviewThreads` showed `line:14, originalLine:8, isOutdated:false`; `gh pr view 58 --json mergeStateStatus` → `BLOCKED`. New duplicate thread `PRRT_kwDOSHZe8M6aJfYW` (different fingerprint) confirmed via the same query. See V4 for a genuine `isOutdated:true` case. |
| V4 | Does a review thread anchored on a since-deleted file still block merge? | **Yes, and worse than expected — now fixed.** Deleting the file made both threads `isOutdated: true, line: null` and `mergeStateStatus` stayed `BLOCKED` (expected). But because the file was added-then-deleted entirely within the PR's own commits, the diff against the merge-base netted to **empty** — and the script used to exit at the old `pr-review-threads.sh:130-134` ("No diff, nothing to review", `exit 0`) *before* the absence-reconciliation loop that would ever mark/auto-resolve a stale thread ran. Any PR whose net diff was empty (for any reason, not just this scenario) could never auto-resolve an existing gating thread. Filed as go-kure/.github#60; fixed in the same PR as #61 (this repo's `fix/60-61-prt-loud-surface-empty-diff` branch): `enforce` mode now falls through an empty net diff as zero findings instead of short-circuiting, so absence reconciliation still runs and a reverted file's thread can reach `SET_FIRST_ABSENT` / `REPLY_RESOLVE` on a later push. `advisory`/`off` keep the cheap exit unchanged — neither ever creates or reconciles a thread. Not re-verified against a live PR by this PR itself — that requires the Live spike below, after both PR1 and PR2 of the pin-bump cycle merge. | 2026-08-18 (found); fix landed pending PR1/PR2 merge | go-kure/.github#58, commit `b9676ad` (file deleted). Job run `32149747984`/job `95752355995`, step log: `"No diff, nothing to review."` at 14:40:21Z, step duration 734ms. `reviewThreads` query showed both threads `isOutdated:true, line:null, isResolved:false`; `mergeStateStatus` still `BLOCKED`. Fix: go-kure/.github#60/#61 PR, `scripts/pr-review-threads.sh` empty-diff fall-through + `scripts/test/pr-review-threads-test.sh` cases 7-9. |
| V5 | What does `POST /pulls/{n}/comments` actually return (status + body) for an out-of-diff `line`, and for `subject_type: file` on a path not present in the PR? | **Both `422 Unprocessable Entity`**, no comment created (no orphaned/floating comment left behind either way). Out-of-diff `line`: `{"errors":[{"resource":"PullRequestReviewComment","field":"pull_request_review_thread.line","message":"could not be resolved"}]}`. `subject_type:file` on a path outside the PR: `{"errors":[{"resource":"PullRequestReviewComment","field":"pull_request_review_thread.path","code":"invalid","message":"could not be resolved"}]}`. | 2026-08-18 | `gh api -X POST repos/go-kure/.github/pulls/58/comments` against commit `c565583`, run directly (not via the script) — both probes returned HTTP 422 with the bodies quoted. |
| V6 | Is GraphQL's `resolvedBy` null when the **bot** itself resolves a thread (vs. a human)? | **Answered, but not the way the question assumed — the bot can never resolve a thread at all.** After #60/#61 landed (org-wide `enforce` mode live), the loud fail-closed exit worked exactly as designed: pushing a fix for the planted finding produced a clean `SET_FIRST_ABSENT` pass with full stage tracing (`prt: fp=... absent -> REPLY_RESOLVE` etc.), confirming #61's fix. But the second absence pass — where the script should call `resolveReviewThread` — failed closed with `REVIEW_INCOMPLETE: fp=...: viewerCanResolve=false, skipping absence auto-close`. GraphQL confirms: the same thread reports `viewerCanResolve:true` to a human PAT and `false` to the `github-actions[bot]` actor that created it, even with the workflow's declared `pull-requests: write` permission. This matches the caveat already recorded at `scripts/lib/prt/gh.sh:18-20` ("need write access or PR authorship, not thread ownership") — the default `GITHUB_TOKEN` apparently doesn't carry the permission GitHub's own resolve mutation checks, a known platform limitation for `GITHUB_TOKEN`-authored review comments (workarounds require a PAT or GitHub App installation token). Practical consequence: **the `resolved_by_bot` branch at `pr-review-threads.sh:438-439` is currently unreachable in production** — a fixed finding will never auto-resolve via this token; a human must resolve it manually every time, same as any other reviewer's open thread. The original V6 question (does `resolvedBy` read null for a bot-driven resolve) is moot until that permission gap is closed. | 2026-08-18 | go-kure/.github#66 (scratch, closed unmerged). Thread `PRRT_kwDOSHZe8M6aQ1BO`: created at commit `a93d8bc` (run `32186029587`/job `95869718496`); `SET_FIRST_ABSENT` at commit `4e3d460` (run `32186204942`/job `95870284112`, clean pass, full `prt:` stage tracing visible); resolve attempt at commit `cb2f91a` (run `32186275118`/job `95870507856`) failed with the quoted `REVIEW_INCOMPLETE` line. GraphQL `reviewThreads` query on the same thread: `viewerCanResolve:true` (human PAT) vs. the job log's `viewerCanResolve=false` (bot). |
| V7 | Does `mode: off` actually reach the script as `off` through the composite action's `mode` input, given `action.yml` only documents `enforce`/`advisory` as its default/example values? | **No — and it doesn't need to.** `mode: enforce` does reach the composite action's `mode` input and `PRT_MODE: enforce` reaches the orchestrator script's env (confirmed via job log) — the input-propagation mechanism itself works for a documented value. But `mode: off` was never observed reaching the script: setting the repo-level variable to `off` made the whole `pr-review` job report `conclusion: skipped` in ~1s, because the job-level `if:` fix from `.github#56`/`#57` skips the job *before* the composite action's `mode` input is even evaluated. So the question's literal premise doesn't arise on the current code path — `off`'s effectiveness as the incident escape hatch is fully explained by the job-level skip alone, independent of whatever the script would have done with an `off` it never receives. | 2026-08-18 | Enforce: run `32149428036`, log line `mode: enforce` / `PRT_MODE: enforce` at 14:37:09-10Z. Off: commit `726e8eb`, run `32150278366` → `conclusion: skipped` in 1s; confirmed via `gh api repos/go-kure/.github/commits/726e8eb/check-runs` → `{"status":"completed","conclusion":"skipped","name":"pr-review / AI Code Review"}`. |
| V8 | V6 follow-up (go-kure/.github#68): is the `viewerCanResolve:false` restriction specific to `GITHUB_TOKEN`, or does it reject **any** bot/App actor — and does a human-owned identity fix it for a thread the identity authors **itself** (not just someone else's, which the V6 human-PAT observation already covered)? | **Any bot/App actor is rejected; a human-owned identity, including one automated via a machine-user PAT, is not.** Two probes: (1) A `kure-bot` GitHub App installation token (minted via `actions/create-github-app-token`, full `pull_requests`/`issues` write scope) created a review comment successfully as `kure-bot[bot]`, then queried that same thread: `viewerCanResolve:false` — identical to `GITHUB_TOKEN`. Confirms the restriction is actor-**type**-based (bot/App), not a scope gap specific to `GITHUB_TOKEN`, so no App-token configuration can route around it. (2) A fine-grained PAT owned by a dedicated human machine-user account (`kure-bot`, a real GitHub user, not an App) created a comment and immediately resolved that same thread: `viewerCanResolve:true`, `resolveReviewThread` succeeded, `resolvedBy.login: "kure-bot"`. **Wired as the fix**: `pr-review.yml` now uses `secrets.KURE_BOT_PAT \|\| github.token` / matching `bot-login`, falling back to today's create-only behavior wherever the secret isn't set — see "Token and bot identity" in `docs/pr-review-threads.md`. **Side finding, cost ~1hr of debugging**: the PAT initially failed all writes to org repos with `403 Resource not accessible by personal access token`, despite correct account-level collaborator permission (`write`) and correct token-level permission grants. Root cause: the token was created with its **resource owner set to the machine-user's personal account**, whose "All repositories" access option only covers repos that account *owns* — not org repos it merely collaborates on. No repo picker for org repos is even offered in that state. Fix: recreate/regenerate the token with the **org itself selected as resource owner** (requires the org's Settings → Personal access tokens policy to allow fine-grained PAT access, which must be enabled first — a separate, easily-conflated setting from the per-token "approve this request" action). | 2026-08-18/2026-08-19 | App probe (disproven): `.github/workflows/probe-kure-bot-token.yml`, scratch PR go-kure/.github#69 (closed, branch deleted), run `32192014535`, thread `PRRT_kwDOSHZe8M6aR_nT` — comment `author.login:"kure-bot[bot]"`, `viewerCanResolve:false`. PAT probe (confirmed): `.github/workflows/probe-kure-bot-pat.yml`, scratch PR go-kure/.github#70 (closed, branch deleted), three runs on the same thread-creation logic — `32194052527`/`32194441946` both `403 Resource not accessible by personal access token` on comment creation (resource-owner misconfigured); `32195595993` (after recreating the PAT with `go-kure` as resource owner) succeeded: thread `PRRT_kwDOSHZe8M6aS3Ja`, `viewerCanResolve:true`, resolve response `{"data":{"resolveReviewThread":{"thread":{"id":"PRRT_kwDOSHZe8M6aS3Ja","isResolved":true,"resolvedBy":{"login":"kure-bot"}}}}}`. |
| V9 | Acceptance test: with the `KURE_BOT_PAT` wiring actually in `pr-review.yml` (not an isolated probe), does the real `scripts/pr-review-threads.sh` two-pass absence mechanism (`SET_FIRST_ABSENT` then `REPLY_RESOLVE`) successfully auto-resolve a finding end to end in `enforce` mode, with `incomplete=0`? | **Yes — full production-path confirmation.** Planted a real shellcheck-detectable bug (unquoted `rm -rf $target/*`), let the reviewer create a gating thread as `kure-bot`, then iterated the fix across three pushes: (1) a partial quoting fix — the model still flagged the file (same fp, decision `NONE`, findings=1 — a persistent model judgment call, not a token/resolve issue); (2) stripped the risky pattern entirely — `chunk 0: review ok (0 findings)`, absence loop: `fp=6f84eb956a6f0a43 absent -> SET_FIRST_ABSENT`, `incomplete=0`; (3) empty commit to trigger the confirming pass — `fp=6f84eb956a6f0a43 absent -> REPLY_RESOLVE`, `incomplete=0`. GraphQL confirms the thread itself: `isResolved:true`, `resolvedBy.login:"kure-bot"`. No `REVIEW_INCOMPLETE` at any point once the finding was actually gone — the exact failure mode from V6 does not recur with the PAT wired. | 2026-08-19 | Scratch PR go-kure/.github#71 (closed, branch `spike/68-pat-live-accept` deleted — both `pr-review.yml` and `pr-review-caller.yml` temporarily edited on that branch only, to point the caller's `uses:` ref at the spike branch instead of `@main` so the modified reusable workflow was actually exercised; neither edit is on the real fix branch). Runs: create `32196009371` (`prt: fp=6f84eb956a6f0a43 -> CREATE`, `PRT_BOT_LOGIN: kure-bot`); partial-fix `32196103582` (`fp=... -> NONE`, findings=1); clean-fix `32196284225` (`review ok (0 findings)`, `SET_FIRST_ABSENT`, incomplete=0); confirm `32196365259` (`REPLY_RESOLVE`, incomplete=0). GraphQL on thread `PRRT_kwDOSHZe8M6aS8eF`: `isResolved:true`, `resolvedBy:{"login":"kure-bot"}`. |

Columns: **Recorded** is the date the answer was actually observed and written here (not the date
this scaffold was created). **Evidence** is a pointer to what was observed — a workflow run URL,
a Checks API response, a specific PR/thread/comment — not a restatement of the answer.

Once filled in, cross-reference the corresponding facts already asserted (and marked
"unverified"/"until a live spike") at:

- `scripts/lib/prt/gh.sh:18-20` — the resolve-permission fact recorded there ("need write access or
  PR authorship, not thread ownership") is now confirmed live by V6: the `github-actions[bot]`
  actor fails exactly that check on its own threads.
- `scripts/pr-review-threads.sh:434-439` — the `resolvedBy`/`resolved_by_bot` comment cited V6 as
  "unverified until a live spike"; updated in this same commit to record that the branch is
  currently unreachable in production (bot-driven resolve never succeeds, see V6).
- **Open follow-up, not filed as an issue by this update**: production `enforce` mode (org-wide
  since 2026-08-18) will never auto-close a gating thread even when the underlying finding is
  fixed — every one requires manual resolution. Fixing this needs a token with real write access
  (a PAT or GitHub App installation token) wired into `pr-review.yml`'s `github-token:` input in
  place of `${{ github.token }}`, which is a real (if scoped) infrastructure change and a separate
  decision from documenting the finding.
